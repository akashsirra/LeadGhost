"use client"
import {useState} from 'react'
import Link from 'next/link'
export default function AuditPage(){const [url,setUrl]=useState('');const [data,setData]=useState<any>(null);const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function run(){setLoading(true);setError('');setData(null);try{const r=await fetch('/api/audit',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url})});const j=await r.json();if(!r.ok)throw Error(j.error);setData(j)}catch(e){setError(e instanceof Error?e.message:'Audit failed')}finally{setLoading(false)}}return <main className="min-h-screen px-5 py-8 md:px-10"><div className="mx-auto max-w-5xl"><Link href="/app" className="text-sm text-[#b7ff3c]">← Dashboard</Link><div className="mt-10"><p className="mono text-xs text-[#b7ff3c]">WEBSITE AUDIT</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Find the leaks.</h1><p className="mt-3 max-w-xl text-[#92929c]">LeadGhost inspects publicly accessible HTML and reports what it can actually observe.</p></div><div className="mt-8 flex flex-col gap-3 sm:flex-row"><input value={url} onChange={e=>setUrl(e.target.value)} onKeyDown={e=>e.key==='Enter'&&run()} placeholder="https://business.com" className="flex-1 rounded-xl border border-[#303037] bg-[#0c0c0f] px-4 py-3 outline-none focus:border-[#b7ff3c]/50"/><button onClick={run} disabled={loading||!url} className="rounded-xl bg-[#b7ff3c] px-6 py-3 font-semibold text-black disabled:opacity-40">{loading?'Scanning…':'Run audit →'}</button></div>{error&&<div className="mt-4 rounded-xl border border-red-500/20 bg-red-500/5 p-4 text-sm text-red-300">{error}</div>}{data&&<div className="mt-8 space-y-5"><div className="rounded-2xl border border-[#24242b] bg-[#0c0c0f] p-6"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs text-[#6f6f79]">ANALYZED</p><h2 className="mt-2 text-xl font-semibold">{data.hostname}</h2><p className="mt-1 text-xs text-[#777780]">{data.facts.title||'No page title'}</p></div><div className="text-left sm:text-right"><div className="text-5xl font-semibold text-[#b7ff3c]">{data.score}</div><div className="text-xs text-[#777780]">heuristic score / 100</div></div></div></div><div className="grid gap-3">{data.checks.map((c:any)=><div key={c.key} className="rounded-xl border border-[#24242b] bg-[#0c0c0f] p-5"><div className="flex justify-between gap-4"><b className="text-sm">{c.label}</b><span className={c.ok?'text-[#b7ff3c]':'text-[#ff8a8a]'}>{c.ok?'PASS':'FIX'}</span></div><p className="mt-2 text-xs text-[#85858f]">{c.evidence}</p>{!c.ok&&<p className="mt-3 text-sm text-[#c7c7ce]">{c.fix}</p>}</div>)}</div><div className="rounded-xl border border-[#2b3420] bg-[#10140b] p-5 text-xs leading-6 text-[#909098]">{data.note}</div></div>}</div></main>}
