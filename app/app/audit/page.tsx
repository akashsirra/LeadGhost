"use client"
import { useState } from 'react'
import Link from 'next/link'

export default function AuditPage(){
 const [url,setUrl]=useState(''); const [data,setData]=useState<any>(null); const [loading,setLoading]=useState(false); const [error,setError]=useState('')
 async function run(){setError('');setData(null);setLoading(true);try{const r=await fetch('/api/audit',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url})});const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j)}catch(e){setError(e instanceof Error?e.message:'Audit failed')}finally{setLoading(false)}}
 return <main className="min-h-screen bg-[#070708] text-white"><header className="border-b border-[#202026]"><div className="mx-auto flex max-w-5xl items-center justify-between px-5 py-4"><Link href="/app" className="font-semibold">← LeadGhost</Link><span className="mono text-xs text-[#777780]">LIVE AUDIT</span></div></header><div className="mx-auto max-w-5xl px-5 py-12"><p className="mono text-xs text-[#b7ff3c]">PROSPECT SCANNER</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Show a business where it's leaking customers.</h1><p className="mt-3 max-w-2xl text-[#8f8f99]">LeadGhost checks publicly accessible website signals and turns them into a sales-ready diagnosis. No fabricated performance claims.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><input value={url} onChange={e=>setUrl(e.target.value)} onKeyDown={e=>e.key==='Enter'&&run()} placeholder="https://business.com" className="flex-1 rounded-xl border border-[#303037] bg-[#0d0d10] px-4 py-4 outline-none focus:border-[#b7ff3c]/50"/><button disabled={loading||!url} onClick={run} className="rounded-xl bg-[#b7ff3c] px-7 py-4 font-semibold text-black disabled:opacity-50">{loading?'Scanning…':'Run audit →'}</button></div>{error&&<div className="mt-4 rounded-xl border border-red-900/50 bg-red-950/20 p-4 text-sm text-red-300">{error}</div>}{data&&<section className="mt-10 grid gap-5 md:grid-cols-[220px_1fr]"><div className="rounded-2xl border border-[#29292f] bg-[#0d0d10] p-6"><p className="text-xs text-[#777780]">HEURISTIC SCORE</p><div className="mt-3 text-6xl font-semibold text-[#b7ff3c]">{data.score}</div><p className="mt-2 text-xs text-[#777780]">/ 100</p><div className="mt-6 text-sm font-medium">{data.title}</div><p className="mt-1 break-all text-xs text-[#666670]">{data.url}</p></div><div className="rounded-2xl border border-[#29292f] bg-[#0d0d10] p-6"><h2 className="font-semibold">What we found</h2><div className="mt-5 space-y-3">{data.checks.map((c:any)=><div key={c.key} className="rounded-xl border border-[#24242b] p-4"><div className="flex items-center justify-between"><span className="text-sm">{c.label}</span><span className={c.ok?'text-[#b7ff3c]':'text-[#ff7979]'}>{c.ok?'PASS':'FIX'}</span></div>{!c.ok&&<p className="mt-2 text-xs leading-5 text-[#85858f]">{c.fix}</p>}</div>)}</div><div className="mt-6 rounded-xl bg-[#151515] p-4 text-xs leading-5 text-[#777780]">{data.note}</div><button className="mt-4 w-full rounded-xl border border-[#333339] px-4 py-3 text-sm hover:bg-white/5">Generate Ghost Preview →</button></div></section>}</div></main>
}